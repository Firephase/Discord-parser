# Your Discord user token (NOT a bot token)
# Get it from Discord web app: DevTools → Network → any request → Authorization header
DISCORD_TOKEN=your_user_token_here

# Optional: override database path
DATABASE_URL=sqlite+aiosqlite:///./data/discord_parser.db

# Web server
HOST=0.0.0.0
PORT=8000
